// App-wide demo constants. Single source of truth for the mock identity and
// ward labels that were previously hardcoded across screens. When auth and
// real ward resolution land, these become derived values instead of literals.

export const MOCK_USER_ID = "demo-user-1";

export const USER_WARD = "4";
export const WARD_LABEL = `Ward ${USER_WARD}`;
export const CITY_LABEL = "Bengaluru";
